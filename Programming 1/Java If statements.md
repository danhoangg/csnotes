Subjects/ Topics: #Programming #Java 

```java
if (condition) {
	//run code
} else if (other condition) {
	//run other code
} else {
	//run code if no conditions met
}
```

### Condition examples:
```java
== //equals to
<, > //lesser/greater than
!= //not equal to
!, &&, || //not, and, or
```
